'use client';

/**
 * Effects Configuration Re-export
 *
 * This file re-exports the effectsConfig and effectCategories from the main effects.ts file.
 * This allows the effects/ directory modules to import these configurations.
 */

// Re-export from the main effects.ts file
export { effectsConfig, effectCategories } from '../effects';
